services:
  simple-contacts:
    build: .
    container_name: simple-contacts-dev
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - FLASK_DEBUG=1
    command: ["python", "-m", "simple_contacts.app_main"]

services:
  simple-contacts:
    image: ghcr.io/dvir001/simple-contacts:latest
    container_name: simple-contacts
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config
